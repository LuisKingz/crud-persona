<form method="post" action="{{ path('app_personas_delete', {'id': persona.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ persona.id) }}">
	<button class="btn btn-danger mt-1">Eliminar</button>
</form>
